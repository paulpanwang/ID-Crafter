# Scripts

This directory is reserved for runnable entry points such as:

- inference launchers
- training launchers
- evaluation helpers
- data preparation utilities

Current files are placeholders so the public repository already has a stable layout before code release.
