# Placeholder evaluation configuration for the future public release.

experiment_name: id_crafter_eval_placeholder
output_dir: outputs/eval

data:
  benchmark_manifest: ""

model:
  checkpoint: ""

metrics:
  - identity_consistency
  - text_alignment
  - temporal_quality
