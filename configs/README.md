# Configs

This directory is reserved for structured experiment configuration files.

Suggested organization:

- `configs/inference/`: inference and demo settings
- `configs/train/`: training and reinforcement learning settings
- `configs/eval/`: evaluation and benchmark settings

The current files are templates only and should be treated as placeholders until the code release is published.
